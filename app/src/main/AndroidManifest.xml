<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <supports-screens
        android:largeScreens="true"
        android:smallScreens="true"
        android:anyDensity="true" />

    <application
        android:name=".Application"
        android:icon="@drawable/icon"
        android:label="@string/app_name">

        <activity
            android:name=".Units"
            android:theme="@android:style/Theme.Black.NoTitleBar"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <!-- Used when picking a result from the search bar's autocomplete list. -->
            <intent-filter>
                <action android:name="de.buttercookie.units.ACTION_USE_UNIT" />
                <data android:mimeType="vnd.android.cursor.item/vnd.de.buttercookie.units.unit_usage" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />

        </activity>

        <activity
            android:name=".UnitList"
            android:theme="@android:style/Theme.Dialog"
            android:exported="false"
            android:launchMode="singleTop">

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />

        </activity>

        <activity
            android:name=".UnitDetails"
            android:theme="@android:style/Theme.Black"
            android:exported="false" />

        <provider
            android:name=".UnitsContentProvider"
            android:authorities="de.buttercookie.units"
            android:exported="false" />

    </application>

    <!-- Uncomment the below permission to trigger generation of the fingerprints.json file.
         Check logcat for the name of the generated file. -->
    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/> -->
</manifest> 
